{
  "type": "object",
  "properties": {
    "refinements": {
      "type": "string",
      "description": "Cumulative refinement log per agent rubric (pipelines/illumination-to-implementation/chat-summarizer.md). Subsequent rounds APPEND; never drop prior entries."
    },
    "scope_changed": {
      "type": "boolean",
      "description": "True if the latest chat round materially altered the scope established by the verifier (re-verification may be warranted)."
    }
  },
  "required": ["refinements", "scope_changed"],
  "additionalProperties": false
}
