{
  "type": "object",
  "properties": {
    "test_result": {
      "type": "string",
      "enum": ["pass", "fail"],
      "description": "Whether the final cycle's tests passed with no unfixed issues"
    },
    "test_summary": {
      "type": "string",
      "description": "Brief summary of what ran and the final outcome."
    },
    "issues_found": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Unfixed issues remaining after all cycles. Empty array means everything found was either fixed or not a real issue."
    },
    "test_render": {
      "type": "string",
      "description": "Markdown render shown verbatim at tmux_confirm_gate per agent rubric (src/cli/agents/tmux-tester.md)."
    }
  },
  "required": ["test_result", "test_summary", "issues_found", "test_render"],
  "additionalProperties": false
}
