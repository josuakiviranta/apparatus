{
  "type": "object",
  "properties": {
    "test_result": {
      "type": "string",
      "enum": ["pass", "fail"],
      "description": "Whether the build and test suite passed"
    },
    "test_summary": {
      "type": "string",
      "description": "Brief summary of what ran and the outcome (1-3 sentences)"
    }
  },
  "required": ["test_result", "test_summary"],
  "additionalProperties": false
}
