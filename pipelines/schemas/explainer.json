{
  "type": "object",
  "properties": {
    "explainer_render": {
      "type": "string",
      "description": "Markdown string with four sections (Currently implemented, What will change, Why it matters, Affected files). Used verbatim in the approval gate label."
    }
  },
  "required": ["explainer_render"],
  "additionalProperties": false
}
