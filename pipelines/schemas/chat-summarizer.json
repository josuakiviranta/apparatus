{
  "type": "object",
  "properties": {
    "refinements": {
      "type": "string",
      "description": "Summary of what was refined or changed during the interactive chat session"
    },
    "scope_changed": {
      "type": "boolean",
      "description": "Whether the scope of the proposed change was materially altered"
    },
    "chat_notes_path": {
      "type": "string",
      "description": "Absolute or project-relative path to the chat-notes.md file read by this summarizer, for downstream nodes that need full session context"
    }
  },
  "required": ["refinements", "scope_changed", "chat_notes_path"],
  "additionalProperties": false
}
