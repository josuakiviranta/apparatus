---
description: Ground in this project — read README.md, CONTEXT.md, VISION.md, and every ADR under docs/adr/.
---

Read the orientation docs at the repo root, in this order:

1. `README.md` — public-facing entry
2. `CONTEXT.md` — domain language and glossary
3. `VISION.md` — mission narrative
4. Every file under `docs/adr/` — decision records (the "why" behind the code). Glob `docs/adr/*.md` and read all of them; do not cherry-pick.

After reading, acknowledge with one terse line per top-level doc (≤10 words each) plus one combined line for the ADRs naming how many you read. Do not summarise contents back at length — the user already knows them; the goal is to load them into your working context.
