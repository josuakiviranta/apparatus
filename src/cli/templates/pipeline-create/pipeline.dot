digraph pipeline_create {
  goal="Scaffold a new ralph pipeline interactively"
  inputs="pipeline_name, pipelines_dir"

  start [shape=Mdiamond]
  done  [shape=Msquare]

  scaffolder [agent="scaffolder", interactive=true, prompt="Scaffold a new pipeline using the provided pipeline_name and pipelines_dir."]

  start -> scaffolder -> done
}
