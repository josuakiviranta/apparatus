# Implementation Plan

No pending work. The previous plan (scenario-tests removal) shipped in v0.1.44 across commits `72685ac` (chunks 1+2: code) and `7cb3a86` (chunk 3: docs + directory deletion).

When new work is queued, replace this file with the next plan.
